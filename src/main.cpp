#include <iostream>// means include input output stream(stream is a sequence of data)

int main()// the int is used to indicate that the function returnss a an integer value which in our case is 0
{
    std::cout<<"hello world";
    return 0; // 0 indicates that the program executed successfully
}